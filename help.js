// ─── اوامر ─────────────────────────────────────────────────────────────────────
const { loadSettings } = require("../Main");

module.exports = {
  name: "اوامر",
  description: "يعرض قائمة الأوامر",
  adminOnly: false,

  async run({ api, event, commands }) {
    const settings = loadSettings();
    const prefix   = settings.prefix || "/";

    const list = [
      "╔══════════════════════╗",
      "║    📋 قائمة الأوامر    ║",
      "╚══════════════════════╝",
      "",
      `البادئة: ${prefix}`,
      "",
      `${prefix}بينغ`,
      "  ← يقيس سرعة الاستجابة",
      "",
      `${prefix}سيرفر`,
      "  ← معلومات السيرفر والذاكرة",
      "",
      `${prefix}اوامر`,
      "  ← هذه القائمة",
      "",
      `${prefix}nm تفعيل [الاسم]`,
      `${prefix}nm ايقاف`,
      "  ← يقفل اسم المجموعة",
      "",
      `${prefix}قفل تفعيل`,
      `${prefix}قفل ايقاف`,
      "  ← يقفل البوت (مشرفون فقط)",
      "",
      `${prefix}محرك رسالة [الرسالة]`,
      `${prefix}محرك وقت [الوقت]`,
      `${prefix}محرك تفعيل`,
      `${prefix}محرك ايقاف`,
      "  ← يرسل رسالة تلقائية بفترة محددة",
      "",
      `${prefix}كنيات تفعيل [الكنية]`,
      `${prefix}كنيات ايقاف`,
      `${prefix}كنيات وقت [الوقت]`,
      "  ← يحمي كنية الأعضاء",
      "",
      `${prefix}cookie`,
      "  ← يحدّث الكوكيز (مشرفون فقط)",
      "",
      "⏱️ الوقت: بالثواني (مثال: 30s) أو بالدقائق (مثال: 5m)",
    ].join("\n");

    api.sendMessage(list, event.threadID);
  },
};
